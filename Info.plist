<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleDisplayName</key>
    <string>CCBar</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSDesktopFolderUsageDescription</key>
    <string>CCBar 只确认 AI 会话记录中项目目录是否存在，不读取文件夹内容，数据不会离开这台 Mac。CCBar only checks whether project folders exist; it never reads their contents.</string>
    <key>NSDocumentsFolderUsageDescription</key>
    <string>CCBar 只确认 AI 会话记录中项目目录是否存在，不读取文件夹内容，数据不会离开这台 Mac。CCBar only checks whether project folders exist; it never reads their contents.</string>
    <key>NSDownloadsFolderUsageDescription</key>
    <string>CCBar 只确认 AI 会话记录中项目目录是否存在，不读取文件夹内容，数据不会离开这台 Mac。CCBar only checks whether project folders exist; it never reads their contents.</string>
    <key>NSMusicFolderUsageDescription</key>
    <string>CCBar 只确认 AI 会话记录中项目目录是否存在，不读取文件夹内容，数据不会离开这台 Mac。CCBar only checks whether project folders exist; it never reads their contents.</string>
    <key>NSPicturesFolderUsageDescription</key>
    <string>CCBar 只确认 AI 会话记录中项目目录是否存在，不读取文件夹内容，数据不会离开这台 Mac。CCBar only checks whether project folders exist; it never reads their contents.</string>
    <key>NSMoviesFolderUsageDescription</key>
    <string>CCBar 只确认 AI 会话记录中项目目录是否存在，不读取文件夹内容，数据不会离开这台 Mac。CCBar only checks whether project folders exist; it never reads their contents.</string>
    <key>NSHumanReadableCopyright</key>
    <string></string>
    <key>NSSupportsAutomaticTermination</key>
    <true/>
    <key>NSSupportsSuddenTermination</key>
    <true/>
</dict>
</plist>
