# cc-bar 界面布局

> 本文档锁**每个界面长什么样**:尺寸、内边距、控件位置、字段、数据来源、空状态。
> 字号 / 颜色 / 圆角 / 间距基线 / 双语词表 / 浅深色对照统一在 [设计风格.md](设计风格.md),这里不重复。下文写到 `设计风格 §X.Y` 时即指那份文档的对应小节。
>
> 锚定的 prototype 变体(`Prototype.html` 推荐组合,见 [历史参考/设计稿/](历史参考/设计稿/)):
> - Popover → **variant A · Vertical list**
> - Floating HUD → **variant 1 · Two-row pill**
> - Statistics → **variant A · Dashboard**(Overview)
> - Preferences → **PrefsV_Main**
> - Onboarding → **OnboardingV_Welcome + OnboardingV_DetectAccounts**

---

## 1. Popover

### 1.1 整体规格

| 项 | 值 |
|---|---|
| 宽 | 340pt(固定) |
| 高 | auto;随启用 Provider 数量增长,零服务 ~110pt(提示空状态) |
| 圆角 | 18pt(`NSPopover` 系统提供,不自绘) |
| 材质 | `.popover`(系统) |
| 锚点 | 菜单栏图标下方,7pt arrow notch 朝上 |
| 出现动画 | 系统默认(`MenuBarExtra(.window)` 提供) |

### 1.2 结构总览(top → bottom)

```
┌───────────────────────────────────────────────────────────┐
│ Header                                       14/12/12pt  │
│   "Usage"  13pt 600                                       │
│   "用量 · refreshed 32s ago"  11pt secondary              │
│                          ● [📊] [↻] [⚙] [⏻]               │
├───────────────────────────────────────────────────────────┤  ← 0.5pt separator full
│ Codex block                                  14/16pt     │
│   ▢ Codex                              resets in 2h 18m   │
│     OpenAI · Plus                                         │
│                                                           │
│   ╭─────╮  Weekly · 周额度                          31%   │
│   │ 42% │  ▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░  (5pt bar)        │
│   │  5H │                                                 │
│   ╰─────╯  184k / 440k        $12.40                      │
│            tokens used        this week                   │
├───────────────────────────────────────────────────────────┤  ← 0.5pt separator inset 16
│ OTHER CODEX ACCOUNTS · 其他 Codex 账号         3           │  ← 仅有导入账号时出现
│   codex-work     5H ▓▓▓▒░░  18%  ↻ 3h 41m               │
│                  WK ▓▓▓▓▓▒░  62%  ↻ 4d 02h               │
│   codex-team     5H ▓▓▓▓▓▓▒  74%  ↻ 0h 52m               │
│                  WK ▓▓▓▓▒░░  48%  ↻ 4d 02h               │
├───────────────────────────────────────────────────────────┤  ← 0.5pt separator inset 16
│ Claude Code block(同上,桃橙色)                           │
├───────────────────────────────────────────────────────────┤  ← 0.5pt separator inset 16
│ Antigravity block(同上,Google Blue;不显示 cost)          │
│   GM ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  100%  ~3h 43m  │
│   GW ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  100%  ~6d 23h  │
└───────────────────────────────────────────────────────────┘
```

> 与原型差异:已移除独立 footer。原 footer 上的「Open Statistics」按钮 和「refresh」按钮合并为 header 右侧的一级图标按钮,刷新进度通过刷新图标自身旋转表达,不再单独画进度条。

### 1.3 Header

- **容器**:HStack(spacing 4),padding `14pt 上 / 12pt 左右 / 12pt 下`(到 separator)。
- **左侧**(VStack,line-height 1.1):
  - `Usage` — 13pt .semibold
  - `用量 · refreshed 32s ago` — 11pt secondary
    - `refreshed {n}s ago` / `{n}m ago` / `{n}h ago`,取所有已启用主 Provider 中**最近**一次成功时间;无成功时显示 `用量 · 等待数据`。
- **右侧**(HStack,spacing 4):
  1. **status dot** — 7pt 圆点(live/stale/offline),颜色按 设计风格 §4.3,hover tooltip `Live · 在线` 等。
  2. **Open Statistics** — `chart.bar.xaxis` 13pt,按钮 `26pt × 22pt` 圆角 5pt,hover 浅灰背景(`Color.primary.opacity(0.08)`)+ 手型光标。点击先设 `appState.mainTab = .stats` 再 `openWindow(id: "main")`,Popover 关闭。
  3. **Refresh** — `arrow.clockwise` 13pt,同尺寸按钮。点击触发 `appState.refreshNow()`,图标旋转 360° / 700ms ease-in-out,刷新中按钮 disabled。
  4. **Settings** — `gearshape` 13pt,同尺寸按钮。点击先设 `appState.mainTab = .settings` 再打开主窗口,与 ⌘, 命令行为一致,总是落到设置 tab。
  5. **Quit** — `power` 13pt,同尺寸。点击 `NSApp.terminate(nil)`,hover tooltip `Quit · 退出`。

> 设计依据:用户反馈一级操作(设置 / 刷新 / 统计 / 退出)藏在 kebab + footer 里不直观,统一拉到顶部图标行,降低点击层级。Quit 早期收在 `ellipsis` 菜单需要点两次,后续也拉平为一级图标。

- **separator**:0.5pt 全宽,`.separatorColor`。

### 1.4 服务 block(Codex / Claude / Antigravity 共用结构)

整个 block 用 `14pt 上下 / 16pt 左右` padding。

#### 1.4.1 Service header row(HStack,gap 9pt)

| 元素 | 规格 |
|---|---|
| 服务 tile | 22pt × 22pt squircle(6pt 圆角),服务识别色填充,白色 logo 14pt 居中。详见 设计风格 §11.2。 |
| 服务名 + plan(VStack,line-height 1.08) | `Codex` 13pt .semibold kerning -0.1<br>`OpenAI · Plus` 10.5pt secondary(plan 取自 `appState.codexAccount?.planType`,缺失时只显示 `OpenAI`) |
| Spacer | — |
| 服务状态圆点 | 6pt 圆,贴 Spacer 后,行尾。颜色按 statuspage indicator(none 绿 / minor 黄 / major 橙 / critical 红 / maintenance 蓝),`unknown` 或未拉到时不显示。受设置项 `showServiceStatus` 控制,tooltip 显示 statuspage description + 相对更新时间。 |
| Reset hint | `resets in 2h 18m` 11pt tabular secondary。取自主要额度的 `resetsAt`。<br>无数据时显示 `reset unknown`。 |

#### 1.4.2 Body row(HStack,gap 14pt,marginTop 12pt)

**左栏:进度环**(56pt 直径,5.5pt 描边,详见 设计风格 §11.3)

- 进度 = `primaryLimit.window.remainingPercent / 100`,起点 12 点钟,顺时针(剩余越多环越满)。
- 中央 VStack(spacing 1,line-height 1):
  - 百分比 14pt .semibold tabular(状态色)
  - 固定短标签，不随 App 语言切换：五小时额度统一显示 `5HOUR`；周额度 Codex 显示 `WEEKLY`,Claude 统一显示 `ALL`(不论该周限额落在主要还是次要额度位置,与 §1.7 一致)。
- 状态色规则按 设计风格 §4.3。

**右栏:weekly + stats**(VStack,flex 1)

1. **Weekly 标题行**(HStack space-between,marginBottom 4pt):
   - 左:`Weekly · 周额度` 10.5pt secondary
   - 右:`31%` 10.5pt tabular(状态色,见 设计风格 §4.3)
2. **Weekly 条**:高 5pt,圆角 2.5pt,状态色填充 + `.quaternarySystemFill` 轨道。进度 = `weekly.remainingPercent / 100`。
3. **Stats 双列**(HStack,gap 14pt,marginTop 9pt;Antigravity 隐藏):
   - 左:`184k / 440k` 11pt tabular .medium primary(主)+ `tokens used` 9.5pt tertiary(副,在主下方,无 ·)
   - 右:`$12.40` 11pt tabular .medium primary + `this week` 9.5pt tertiary

> Prototype 这里**没有**写中文副标(`tokens used` / `this week` 是英文单语)。但我们用 设计风格 §5.4 词表:实际渲染 `tokens used · 已用 tokens` 二行 / `this week · 本周` 二行,字号顶部 11pt,中文 9.5pt tertiary 紧贴下一行。

#### 1.4.3 Block 间分隔

- 相邻 Provider 之间:0.5pt 横线,**左右各 inset 16pt**。
- 最后一个 block 之后无 footer,由系统 `NSPopover` 圆角承接底边。

#### 1.4.4 空状态 / 错误状态

- `appState.codexQuota == nil` 且 `codexQuotaError == nil`:整个右栏替换为 `Waiting for data · 等待数据` caption secondary 居左;环显示空轨道 + 中央 `--%`。
- `codexQuotaError != nil`:右栏底部追加一行,`Color.orange` + 前置 `exclamationmark.triangle.fill` 图标,显示错误首行(`shortError` 截断 110 字)。环按最后一次成功快照继续显示(`appState.codexQuota` 不清空)。其他账号行的错误态样式相同。
- 服务在 Settings 里被关闭(`showCodex == false`):**整个 block 不渲染**。
- 全部服务关闭:Popover 只显示 header + 中部一段 `No services enabled · 未启用任何服务` caption,提示去 Settings。

#### 1.4.5 Antigravity 差异

- 展示两组独立额度：主区块为 `Claude + GPT models` 的 5H/WK（与 Codex/Claude 格式一致，菜单栏和悬浮窗仅使用此数据），底部额外展示 GM 行（`Gemini Models` 5h）和 GW 行（`Gemini Models` 周），风格对齐 `weeklyRow`（9pt 标签 + 2.5pt 进度条 + 百分比 + 重置时间）。
- 服务 tile 使用 `AntigravityAccent`;副标题显示 Google 账号邮箱与 plan,隐私模式下隐藏邮箱。
- 不显示 today / this week cost;App 未运行或 IDE 只能提供部分数据时保留缓存并显示短错误。
- `Gemini Models` 组数据仅影响 Popover 展示，菜单栏百分比、悬浮窗和统计页仍只使用 `Claude + GPT models` 的 5H/WK。

### 1.5 Footer(已废弃)

> 原方案的 footer(左侧 `Open Statistics` 按钮 + 右侧 refresh + kebab)已**完全移除**。所有一级操作上移到 §1.3 Header 的图标行,刷新进度通过 header 刷新图标自身旋转 + disabled 表达。本节保留仅为历史参考,新实现不要再引入 footer。

### 1.6 交互

| 触发 | 行为 |
|---|---|
| 点 header `chart.bar.xaxis` | `mainTab = .stats` 后 `openWindow(id: "main")`,Popover 关闭 |
| 点 header `arrow.clockwise` | 触发 `appState.refreshNow()`,旋转 360° / 700ms,最低保持 0.5s 防闪 |
| 点 header `gearshape` | `mainTab = .settings` 后打开主窗口 |
| 点 header `power` | `NSApp.terminate(nil)`,退出 App |
| 点 service block 任意空白 | 暂不响应(后续可定位主窗口到对应服务 scope) |
| 点 Popover 外 | Popover 关闭(系统默认) |
| Esc | Popover 关闭(系统默认) |
| ⌘R(focus 在 Popover) | 触发 refresh |
| 鼠标悬停任意图标按钮 | 浅灰圆角背景 + 手型光标(详见 设计风格 §11.9) |
| 鼠标悬停 reset 时间 | 行内切换相反格式:设置为剩余时长时显示具体时刻(当天 `HH:mm`,跨天 `MM-dd HH:mm`);设置为具体时间时显示剩余时长。hover 热区略大于文字本身,避免鼠标遮挡数字才触发。 |

### 1.7 数据来源对照

| UI 字段 | 数据源 |
|---|---|
| 主要额度百分比(环) | `appState.{codex,claude}Quota?.primaryLimit.window.remainingPercent` |
| 主要额度标签 | `primaryLimit.kind` 映射为固定短标签：五小时 `5HOUR`；周额度 Codex 为 `WEEKLY`,Claude 为 `ALL`,不随 App 语言切换 |
| 主要额度 reset hint | 同上 `.resetsAt`；缺失时只延用尚未过期的同限额缓存值 |
| 次要额度 + 模型额度 | `secondaryLimit`（去重后）+ Claude `modelLimits` |
| `{used}k / {cap}k tokens` | `weekly.used / weekly.cap`(格式化见 `StatsFormatter.token`) |
| `${spend} this week` | 周 spend(从 `UsageAggregator` 取本周 cost 总和) |
| Header refreshed time | 所有已启用服务 `refreshState.lastSuccessAt` 取 max |
| 状态点 live/stale/offline | 现有 `headerState` 三态判定 |
| Plan 副标(`OpenAI · Plus`) | `appState.{codex,claude,antigravity}Account?.planType / subscriptionType` |
| 服务 tile logo | `Resources/Assets.xcassets/codex.svg / claude.svg / antigravity.svg` |
| GM 行(Gemini 5h) | `appState.antigravityQuota?.geminiWindow` |
| GW 行(Gemini 周) | `appState.antigravityQuota?.geminiWeekly` |

---

## 2. Floating HUD

### 2.1 整体规格(默认变体:Two-row pill)

| 项 | 值 |
|---|---|
| 宽 | auto,最小 168pt |
| 高 | auto(两行 pill 约 50–60pt) |
| 圆角 | 14pt |
| 材质 | `.hudWindow`(`NSVisualEffectView`,`blendingMode: .behindWindow`) |
| 背景 alpha | 浅 0.58 / 深 0.62(material 自带) |
| 阴影 | `0 10 28 rgba(0,0,0,0.28)` + inset 0.5pt `rgba(255,255,255,0.1)` 高光(material 自带 + SwiftUI 叠一层 fallback 见 设计风格 §7) |
| 内边距 | 10pt 上下 / 14pt 左右 |

### 2.2 内容结构(variant 1)

VStack,spacing 7pt。每行结构如下:

```
[●]  ████████░░░░░░░░░░  [42%]
[●]  ███████████████░░░  [78%]
 18pt    flex,4pt bar     34pt
```

| 元素 | 规格 |
|---|---|
| 左侧 logo tile | `ServiceTile` 18pt 圆角 5pt,服务色填充 + 内嵌 logo 12pt(`codex.svg` / `claude.svg`),logo 加载失败时回退字母 `C` / `K` |
| 中间 bar | 高 4pt,圆角 2pt,状态色填充 + `.quaternarySystemFill` 轨道,flex 1(最小宽 56pt) |
| 右侧百分比 | 13pt .semibold tabular kerning -0.3,状态色(见 设计风格 §4.3),最小宽 34pt 右对齐 |
| 行内 spacing | 8pt(tile / bar / 百分比 三段之间) |

进度 = `primaryLimit.window.remainingPercent / 100`。bar + 文字均按 设计风格 §4.3 四档状态色显示。

### 2.3 备选变体(Settings 提供切换)

prototype 还画了另外 7 个变体,Settings → Floating HUD → Style 下拉里全部提供:

| Variant | 名称 | 适用场景 |
|---|---|---|
| 1(默认) | Two-row pill · 两行胶囊 | 标准 |
| 2 | Stacked numbers · 大数字 | 不要 bar,只看百分比 |
| 3 | Dual mini rings · 双小环 | 视觉化偏好者 |
| 4 | Ultra-thin · 极简 | 屏幕空间紧张 |
| 5 | Single service · 单服务 | 只关心 Claude / 只关心 Codex |
| 6 | Horizontal slim · 横向窄条 | 顶部 / 底部窄边贴边 |
| 7 | 5h + Week verbose · 详细模式 | 同时看 5h 和周 |
| 8 | No chrome · 无背景纯文字 | 极致简约,文字带阴影直接铺桌面 |

实现:每个 variant 一个 `View`,在 `FloatingContentView` 里按 settings 切换。

### 2.4 交互

| 触发 | 行为 |
|---|---|
| 整个 HUD 区域拖动 | 移动位置,松手时持久化到 SettingsStore |
| 拖至屏幕边 20pt 内 | 吸附到边缘 |
| 鼠标进入 / 离开 | 透明度根据 Settings → Opacity when idle 切换(idle 0.7 → hover 1.0,150ms ease) |
| 点击单行 | 暂不响应(规划中:跳转主窗口对应服务 scope) |
| 右键 | 显示原生菜单:`Hide HUD · 隐藏` / `Settings · 设置` |

### 2.5 浅深色

- **不写**手动 background;`NSVisualEffectView(.hudWindow)` 在两种 appearance 下自动给出 prototype 的两套 alpha + blur。
- 文字颜色:标签 `.secondary`、百分比状态色(见 设计风格 §4.3 / §12.1)。
- **不**加 strokeBorder。当前 `Floating/FloatingContentView.swift:35-38` 的 `strokeBorder(opacity 0.08)` 应去掉。

### 2.6 数据来源对照

| UI 字段 | 数据源 |
|---|---|
| 行 1 百分比 + bar | `appState.codexQuota?.primaryWindow.remainingPercent` |
| 行 2 百分比 + bar | `appState.claudeQuota?.primaryWindow.remainingPercent` |
| 行 3 百分比 + bar | `appState.antigravityQuota?.primaryWindow.remainingPercent` |
| 显示 / 隐藏 | `settings.floatingEnabled` |
| 哪些服务 | `settings.isProviderShownInFloatingHUD(_:)` |
| Variant | `settings.floatingVariant`(新字段,enum,默认 `.twoRowPill`) |
| 位置 | `settings.floatingPosition`(CGPoint,持久化) |
| Idle 透明度 | `settings.floatingIdleOpacity`(Double,默认 0.7) |

---

## 3. Statistics 主窗口

系统级入口：从 Dock、应用程序、Launchpad 或 Raycast 启动 / 重新打开 CCBar 时，主窗口置前并切到 Statistics；`SMAppService` 开机自动启动保持静默，首次启动仍优先显示 Onboarding。

### 3.1 整体规格(默认视图:Overview)

| 项 | 值 |
|---|---|
| 宽 / 高 | 默认 1080pt × 680pt,最小 800pt × 520pt,可调 |
| 圆角 | 12pt(系统 Window) |
| 工具栏 | 系统 toolbar |
| 布局 | Sidebar(200pt) + Main canvas |

### 3.2 Toolbar

- **左**:`Statistics` 13pt .semibold + `用量统计` 11pt secondary,水平间距 10pt。
- **右**:SegmentedControl,9 个段:
  `Today / Yesterday / Week / Month / Year / 7d / 30d / All / Custom`
  对应 03 词表:`今天 / 昨天 / 本周 / 本月 / 本年 / 7 天 / 30 天 / 全部 / 自定义`(中文不显示在控件里,而是在 Sidebar Range 组里显示)。
  - 容器:2pt padding,圆角 7pt,bg `rgba(0,0,0,0.06)`(浅)/ `rgba(255,255,255,0.08)`(深)
  - 段:`3pt 上下 / 10pt 左右`,高 22pt,圆角 5pt
  - active:浅色 `#fff` + 微阴影 `0 0.5 2 rgba(0,0,0,0.18)`;深色 `rgba(120,120,128,0.55)` + 同阴影;字体 .semibold,letterSpacing -0.05
  - **SwiftUI 推荐**:直接用 `Picker(.segmented)`。如果系统外观不够还原,再自绘。

### 3.3 Sidebar(200pt 宽)

- 整体:`.sidebar` material,padding `14pt 上下 / 12pt 左右`,内部 VStack gap 16pt。
- 右侧 0.5pt separator(分隔 sidebar 与 main)。

**三个 Group**,每个 group:

- **Group title**:10pt .semibold letterSpacing 0.4 tertiary 大写,padding `0pt 6pt 4pt 6pt`,格式 `RANGE · 时间范围`。
- **Items**(VStack,gap 1pt):每行 `5pt 上下 / 8pt 左右`,圆角 6pt,12pt .medium,**active 行**:accent 背景 + 白字。

| Group | Items(EN / CN) | 说明 |
|---|---|---|
| Range · 时间范围 | Today / Yesterday / This week / This month / This year / Last 7 days / Last 30 days / All time / Custom… | 与 toolbar segmented 同步;每行带一个 13pt 日历 icon |
| Service · 服务 | All / Codex / Claude Code | All 用 8pt 空占位;Codex / Claude 用 8×8 squircle(radius 2,服务色)作为前缀色块 |
| View · 视图 | Overview / Conversations / Timeline | 各带 13pt icon:`rectangle.split.2x2` / `bubble.left.and.bubble.right` / `chart.line.uptrend.xyaxis` |

每行**右侧**有 10.5pt tertiary 的中文 caption(active 行 opacity 0.75,非 active 0.5)。

### 3.4 Main canvas — Overview(主视图)

整体 padding 20pt,VStack gap 12pt(KPI 行下 18pt)。

模块顺序:KPI 行 → Token 拆分 + 按服务 → 每日用量 → 按模型。

`Token breakdown` 与 `By service` 两个面板始终上下堆叠,每个面板占满主画布宽度,高度由各自内容自然撑开。`StatsView.wideCanvasWidth` 仅用于 Timeline 视图的宽度断点。

#### 3.4.1 顶部 KPI 行(4 卡)

Grid `repeat(4, 1fr)`,gap 12pt。每卡:

| 项 | 值 |
|---|---|
| 圆角 | 10pt |
| 背景 | 浅 `#fff` / 深 `rgba(60,60,63,0.4)`(见 设计风格 §12.3) |
| 内描边 | 0.5pt(见 设计风格 §12.3) |
| Padding | 11pt 上下 / 14pt 左右 |

内结构(VStack):

1. **Label 行**(HStack,baseline,gap 4):
   - 可选 6×6 squircle radius 1.5 服务色块(仅服务卡有)
   - `Label` 11pt secondary
   - `Cn` 10pt tertiary(中文 caption)
2. **Value 行**(HStack,baseline,gap 8,marginTop 4):
   - 主值 22pt .semibold kerning -0.5 tabular(服务卡用服务色,合计卡用 primary)
   - Delta 11pt .semibold tabular,红涨绿跌,格式 `↑ 12.4%`(red) / `↓ 2.3%`(green),**裸文字无 chip**

四张卡内容:

| Card | Label / CN | Value | Delta |
|---|---|---|---|
| 1 | `Total tokens` / `总令牌` | `48.2M` | `↑ 12.4%` |
| 2 | `Total spend` / `总花费` | `$1,284.50` | `↑ 8.1%` |
| 3 | `Codex` / `Codex`(灰色块) | `$412.30` 服务色 | `↓ 2.3%` |
| 4 | `Claude Code` / `Claude Code`(桃色块) | `$872.20` 服务色 | `↑ 18.6%` |

Delta 数据 = 当前 range 总值 vs 上一个等长 range 总值。`delta == 0` 时不渲染箭头(避免"没有变化"却显示红色 `↑ 0.0%`)。

Sidebar Service filter 筛中单一服务时,另一服务的卡片(卡 3 或卡 4)整体 `opacity(0.35)`,数值替换为 `—` 占位,不再显示误导性的 `$0.00`;delta 同时置空。

#### 3.4.1a Token breakdown panel(KPI 行下方,独占一行)

容器 `Panel`(同 Daily usage panel 规格)。展示范围随 sidebar 的服务过滤(全部 / Codex / Claude)联动。

- **Title 行**:`Token breakdown` 12.5pt .semibold + `Token 拆分` 10.5pt tertiary。
- **迷你堆叠条**(高 8pt,Capsule,轨道 `Color.secondary.opacity(0.18)`):三段无间隙拼接,依次为输入 / 输出 / 缓存命中,用 `Color.primary` 不同透明度区分(0.85 / 0.6 / 0.4),按三类各自 token 数占比分配宽度。
- **分项**(HStack 横排 4 列,等宽):每列上方为 6pt 圆点(同堆叠条对应透明度)+ 标签 10pt secondary,下方为 12.5pt .semibold tabular 数值;依次为输入 / 输出 / 缓存命中(即缓存读取,`UsageTotals.cacheReadTokens`,均用 `StatsFormatter.compactToken`)和命中率(无可见圆点,保留透明占位对齐,数值 `Color.green`,与 KPI delta 的"绿=好事"同一套语义,不是额度状态色)。面板高度由内容自然撑开。
- **Fast 分项**：仅当所选范围内 `fast.requestCount > 0` 时渲染,渲染时含上方 Divider;横排 5 列，依次为 Fast 原始 Tokens、计费等效 Tokens、倍率（单一倍率或已用模型的倍率范围）、Fast 估算费用、Fast 占比。未收录倍率时等效 Tokens 为 0、倍率显示 `—`；未收录费率时费用显示 `$0`。以上字段不改变 KPI 的原始总 Tokens。没有 Fast 用量时整行(含 Divider)不渲染,而不是显示一整行 0 值。

> 概览页不再展示「总 Tokens / 缓存命中率」的 hero 大数字——总 Tokens 与 KPI 卡 1 完全重复,实现上走 `TokenBreakdownView(showsHero: false)`;对话明细页(§3.6)继续使用 hero 版(`showsHero` 默认 true)。
> 缓存写入(创建)不展示——量级小,且 Codex/OpenAI 协议压根不上报,故所有筛选下都不出现这一项,不再用 N/A 或悬浮提示处理。

#### 3.4.2 Daily usage panel

容器 `Panel`(圆角 12pt,padding 16pt,背景同 KPI 卡)。

- **Title 行**:
  - 左:`Daily usage` 12.5pt .semibold + `每日用量` 10.5pt tertiary(VStack 紧贴)
  - 右:两个图例 chip:
    - `■ Codex` 9pt squircle radius 2 + 11pt secondary
    - `■ Claude Code` 同上
  - 上下文模式(见下)时,图例左侧追加注释 `Last 14 days · highlighted = selected · 近 14 天 · 高亮为所选范围` 10.5pt tertiary。
- **Chart**:高 160pt,堆叠柱图,见 设计风格 §11.6 / `StackedBarChart`。
- **单日上下文窗口**:所选范围只有一天(今天 / 昨天 / 单日自定义,跨度按 ≤1.5 天判断以容忍夏令时)时,图表窗口自动扩展为「范围结束日往前 14 天」:范围内柱子全彩,范围外上下文柱降至 35% 透明。仅图表展示窗口扩展,KPI 与其他面板口径不变;hover 明细在上下文柱上照常可用。
- **Date axis**:6pt 上 margin,5 个等距标签(`Jul 16 / Jul 23 / Jul 30 / Aug 06 / Today`),10pt tertiary mono。
- **Hover(悬浮明细)**:鼠标在图表区移动时按 `chartXSelection` 吸附到最近一天——该天两段柱保持原识别色、其余柱降至 35% 透明,并叠一条竖向 `RuleMark`;柱顶弹出浮层卡片(`.regularMaterial` + 圆角 8pt + 0.5pt 描边,宽度 200pt,`fit(to: .plot)` 防溢出),内容为「日期 / ■ Codex 花费 / ■ Claude 花费 / 分隔线 / 合计花费 / 合计 Tokens / 分隔线 / 输入 / 输出 / 缓存命中 / 命中率」,均为整行单栏(标签靠左、数值靠右,`Spacer` 撑开),金额与 token 均 `monospacedDigit`;各服务金额、合计花费和合计 Tokens 使用 `.semibold` + `.primary`,输入 / 输出 / 缓存命中 / 命中率保持 10.5pt regular secondary。金额不套状态色。移出图表区浮层消失;切换时间范围或服务过滤时重置选中。

#### 3.4.3 By service panel(独占一行)

- Panel title:`By service · 按服务`(同上格式)
- 内容使用 HStack 左右两列(gap 16pt),Codex 和 Claude Code 各占可用宽度的 50%。每列为一个 `ByServiceRow`(padding 8pt 上下):
  - 顶行(HStack baseline):
    - 8×8 squircle radius 2 服务色 + 8pt margin
    - `Codex` 12pt .semibold
    - `OpenAI` 10.5pt tertiary,左 6pt
    - 右对齐:`32%` 13pt .semibold tabular + `of spend · 占比` 10.5pt secondary
  - 中间(marginTop 6pt,marginLeft 16pt):Bar 高 5pt 圆角 2.5pt 服务色
  - 第三行(marginTop 4pt,marginLeft 16pt):`18.4M Tokens` 10.5pt .semibold tabular primary
  - 第四行(marginTop 4pt,marginLeft 16pt):`TokenBreakdownInlineRow`(入 / 出 / 缓存命中 / 命中率,10.5pt,Codex / Claude 行都一样,不区分)
  - 有 Fast 请求时增加第五行紧凑明细：闪电图标 + Fast 原始 Tokens + 计费等效 Tokens + Fast 估算费用；纯 Standard 时不占高度。

> 花费金额不再出现在本面板——与 KPI 行的 Codex / Claude Code 卡完全重复;花费占比数字上移到顶行右侧,继续作为 Bar 的标注。

原双栏区右侧的 Current limits panel(当前限额环形图)已移除:该数据在 Popover 中已有展示,用量统计页无需重复。

#### 3.4.4 By model panel(marginTop 12pt)

- Panel title:`By model · 按模型`(同上格式)
- 按服务分组(过滤为「全部」时 Codex 组在上、Claude 组在下,中间 `Divider`;单服务过滤时只显示对应组):
  - 组头:6×6 squircle radius 1.5 服务色 + 组名大写(`CODEX` / `CLAUDE CODE`)10pt .semibold kerning 0.4 tertiary
  - 每个模型一行(VStack,marginLeft 12pt):
    - 顶行(HStack):模型名 12.5pt,右侧依次 `X Tokens`(10.5pt .semibold primary tabular,总量)+ 费用 12.5pt .semibold primary tabular——总 Tokens 和金额是首要信息,与底部分项形成字重层级
    - 底行:`TokenBreakdownInlineRow`(同 By service)
    - 有 Fast 请求时追加紧凑 Fast 明细行，规则同 By service。
  - 无数据时显示 `No data · 无数据` 11pt secondary。

### 3.5 Main canvas — Timeline(今日主要额度变动)

切换到 sidebar → View → Timeline 时显示。该视图只看今天,不展示 Overview 顶部的日期范围 segmented control。

顶部标题行:

- 左侧: `Today's Primary Quota · 今日主要额度` 18pt .semibold。
- 副标题: `Only quota changes are shown. · 仅展示额度发生变化的时间点。` 11.5pt secondary。
- 右侧:当天日期 `yyyy-MM-dd`,11pt monospaced secondary。

账号分区顺序固定:

1. Codex 主账号。
2. Codex 副账号,按设置页排序。
3. Claude Code。

> 展示层去重:主账号段已展示时,与它同身份的镜像导入副账号段不再重复出现(判据 `AppState.importedCodexAccountMirrorsPrimary`,详见技术实现 §14.8)。历史数据仍在盘上,只是不展示。

受 sidebar Service filter 影响:

- All:显示全部分区。
- Codex:只显示 Codex 主账号 + Codex 副账号。
- Claude:只显示 Claude Code。

每个账号分区一个 `ccPanel(cornerRadius: 12)`,padding 16pt,VStack gap 14pt。

> **宽度断点**:主画布宽 ≥ 880pt 时,分区内折线图与变动表格左右并排(HStack gap 14pt,顶对齐):表格固定宽 384pt,折线图占剩余宽度、高 180pt;更窄时回落为上图下表。断点常量与 Overview 共用(`StatsView.wideCanvasWidth`)。

#### 3.5.1 账号分区 header

HStack:

- 左:8×8 服务色 squircle + 账号名 13pt .semibold。
  - Codex 主账号:`Codex`(不再出现「主账号」字样)。
  - Codex 副账号:隐私模式下 `Codex · 账号 N`;否则 `Codex · {alias/email prefix/id}`。
  - Claude:`Claude Code`。
- 右侧 3 个 metric,每项 VStack 右对齐:
  - `Current / 当前`:最近采样或当前快照的主要额度剩余整数百分比；账号名旁显示 5H / WK / CURRENT 类型胶囊。
  - `Today / 今日`:今日所有变动事件 delta 之和,格式 `-11% / +88% / 0%`。
  - `Latest / 最近`:最近一次变动时间 `HH:mm`,无变动为 `--`。

#### 3.5.2 变动折线图

仅当该账号今天有变动事件时显示;无变动时显示居中 `No changes today · 今天暂无变动`,高度 120pt。

- 图表高度 180pt,Swift Charts;整体左侧内缩 8pt,Y 轴刻度不贴面板内容左缘。
- X 轴:变动时间 `HH:mm`。
- Y 轴:当前主要额度剩余百分比,固定 `0...100`,刻度 `0 / 20 / 50 / 80 / 100`。窗口类型改变时重置该账号当天曲线基准。
- 线:账号服务识别色,1.8pt。
- 点:每次变动事件,颜色按变动后剩余百分比走 `statusColor`。
- 图上只画变动点;同值刷新不产生点。

#### 3.5.3 变动表格

折线图下方展示同一账号的变动事件,按时间倒序。

列:

| 列 | 宽度 | 内容 |
|---|---:|---|
| Time / 时间 | 82pt | 变动时间 `HH:mm` |
| Change / 变动值 | 82pt | `after - before`,如 `-12%` / `+59%` |
| After / 变动后剩余 | 104pt | 变动后的主要额度剩余整数百分比 |
| Reset / 重置时间 | 96pt | 该次主要额度的重置时间 `HH:mm`,未知为 `--` |

表头 10.5pt .semibold tertiary,行文本 11.5pt monospaced secondary。负变动红色,正变动绿色;变动后剩余按状态色显示。

行数超过 8 时,表体固定高度(8 行 × 29pt ≈ 232pt)内部滚动,表头固定不随行滚动;不超过 8 行时按内容自适应高度、无内滚。

### 3.6 Main canvas — Conversations（对话）

- 整体为列表＋右侧详情的 `HSplitView`，列表建议宽 390–460pt，详情最小 390pt。
- 顶部工具条：标题/项目搜索、单选项目菜单、排序（最近 / Tokens / 费用）、上次扫描相对时间、本地刷新、时间范围菜单；Codex / Claude Code 继续复用左侧服务筛选（`StatsServiceFilter`，中英文均用产品名，不用公司名），自定义范围时在列表顶部显示两个紧凑 DatePicker。
- 项目菜单受当前时间范围和服务约束，包含最近使用、全部项目、已不存在项目、无明确项目和 CCBar 系统任务；项目名重名时补父路径，菜单项显示范围内对话数，悬停显示完整路径和状态。
- 列表默认按最近活动倒序，每行包含服务标识、标题、日期时间、项目尾段、模型、速度徽标、范围内请求数、Tokens 与 API 等值估算费用；全 Fast 显示 `Fast`，同时包含 Fast 与其他档位显示 `Mixed`，纯 Standard / 仅未识别档位不显示徽标。默认 200 行，底部“显示更多”每次增加 200。
- 右侧详情始终标注 `All time · 全部时间`(hover 提示”该详情展示该对话的全部时间数据,不受上方时间范围筛选影响”,不随列表的时间范围筛选变化)，展示服务、标题、可复制对话 ID、规范项目完整路径 / 特殊归属、分支、开始/结束时间、持续时间(整 1 天时英文按单复数显示 `1 day` / `2 days`)与”包含子任务用量”提示。
- Token 区使用 `TokenBreakdownView` 的 hero 版（总 Tokens、缓存命中率大数字 + 堆叠条,`showsHero` 默认 true；概览页则为无 hero 的紧凑版）；下方补输入、输出、缓存读取、缓存写入、请求数。所有 Token 数值统一走 `StatsFormatter.compactToken`，中文大数显示为 `万 / 亿`、英文显示为 `k / M / B`；请求数保留千分位完整数字。Codex 日志未提供缓存写入时显示 `N/A`。
- 速度档位区展示 Standard / Fast / 未识别三档的原始 Tokens 与请求数，以及 Fast 计费等效 Tokens；各类 Token 同样使用紧凑单位，未知档位只在此处说明。
- 费用区标题固定为 `Estimated cost breakdown · 估算费用构成`，先列 Standard / Fast 费用，再展示按最终档位价格汇总的四项成本和合计；无可靠价格的部分按 `$0` 汇总，不追加“未定价”。
- 按模型区逐行展示模型、速度徽标、总 Tokens、输入/输出/缓存读取和估算费用；有 Fast 时追加紧凑 Fast 原始 Tokens、计费等效 Tokens和 Fast 费用。
- 刷新按钮只调用本地增量日志扫描；扫描中保留旧内容并显示 `ProgressView`，首次无缓存时显示“正在整理历史对话”。

### 3.7 Custom range

点击 toolbar / sidebar 的 `Custom` 后,toolbar segmented 下方滑出一行内联 DatePicker:

```
[From · 起 ▾] [To · 止 ▾]                  [Cancel] [Apply]
```

- 两个 `.compact` DatePicker
- 右侧 `Cancel · 取消`(ghost)+ `Apply · 应用`(primary,filled accent)
- 按下 Apply 后 range 切到 .custom 并 hide。

### 3.8 浅深色

- 整窗口 background 用系统 Window(浅 `#ECECEE` / 深 `#1E1E1F`)。
- Sidebar 用 `.sidebar` material(自动适配)。
- Panel / KPI 卡背景按 设计风格 §12.3 手动切换。
- 堆叠柱图 Codex 段按外观切换灰深浅;Claude 段强制 `#D97757`(见 设计风格 §12.3 例外)。

### 3.9 数据来源对照

| UI 字段 | 数据源 |
|---|---|
| Range 切换 | `@State range: StatsRange`(已有) |
| KPI cards | `UsageAggregator.totals(...)`(已有)+ 新增 `previousTotals(...)` 对比 |
| Daily usage 堆叠柱图 | `UsageAggregator.snapshot()` 按 day 聚合两服务(已有 `dailyRows` 雏形) |
| By service | `UsageAggregator.totals(app:from:to:)` 双服务 |
| Current limits | `appState.{codex,claude}Quota?.{primaryLimit, secondaryLimit, modelLimits}` |
| Timeline 账号分区 | `appState.quotaHistory.lastSamples` + 当前 `appState` 账号列表 |
| Timeline 折线图 / 表格 | `appState.quotaHistory.events` 按账号 key 过滤 |

---

## 4. Preferences(主窗口 Settings tab)

### 4.1 整体规格

| 项 | 值 |
|---|---|
| 宽 / 高 | 680pt × 780pt 默认 |
| Toolbar 标题 | `Preferences` 13pt .semibold + `偏好设置` 11pt secondary |
| 内容区 padding | 20pt 上下 / 28pt 左右,可滚动 |
| Group 间距 | marginBottom 22pt |

> 实现采用自绘 `PrefsGroup` + `PrefsRow` 结构(定义在 `Settings/SettingsRootView.swift` 底部),**不**用 Form .grouped。理由:Form .grouped 在 macOS 上的视觉密度和样式不可控,与 prototype 差距太大。

### 4.2 PrefsGroup 结构

每组(在 Accounts / Menu Bar / Floating HUD / Refresh / General 各一份):

```
Title · 中文                                            12pt 600
desc · 中文 desc                                        10.5pt tertiary
┌────────────────────────────────────────────────────────────┐
│ Row 1                                                      │
├────────────────────────────────────────────────────────────┤
│ Row 2                                                      │
├────────────────────────────────────────────────────────────┤
│ ...                                                        │
└────────────────────────────────────────────────────────────┘
```

- **Group header**(padding `0 4pt 8pt`):
  - 第一行:`Accounts · 账号` 12pt .semibold letterSpacing -0.05
  - 第二行:`Auto-detected on your Mac. Toggle which services to display. · 自动检测,自行勾选要显示的` 10.5pt tertiary,marginTop 2pt
- **Group body**:
  - 圆角 10pt
  - 背景按 设计风格 §12.3 切换(浅 `#fff` / 深 `rgba(60,60,63,0.4)`)
  - 0.5pt 内描边(同 §12.3)
  - 内含若干 `PrefsRow`,**每行之间一条 0.5pt separator**(行的 borderTop,首行也加)
  - overflow hidden

### 4.3 PrefsRow 结构

每行 padding `10pt 上下 / 14pt 左右`,HStack gap 12pt:

- **左侧 VStack flex 1**(line-height 1.25):
  - `Label` 12.5pt .regular + `· 中文` 11pt tertiary(同行 inline)
  - 可选 desc 10.5pt tertiary marginTop 1pt
- **右侧控件**(flexShrink 0):Toggle / DropdownPill / CheckboxGroup / RadioGroup / Slider 之一

### 4.4 控件库

#### Toggle(macOS Switch)

- 32pt × 20pt,圆角 11pt
- 关闭态:`.fill`(`rgba(120,120,128,0.16)` 浅 / 0.24 深)+ inset 0.5pt strong separator
- 打开态:`Color.green`(系统语义)
- 内部 thumb:16pt × 16pt 白圆,阴影 `0 1 3 rgba(0,0,0,0.3)` + inset 0.5pt 0.05 黑描边
- 150ms ease 切换

**SwiftUI 推荐**:直接用 `Toggle(...).toggleStyle(.switch)`,系统已经长这样,无需自绘。

#### DropdownPill

- 22pt 高 / 8pt 左右 padding / 圆角 6pt
- 浅:`#fff` + inset 0.5pt 0.16 黑描边 + `0 0.5 1.5 rgba(0,0,0,0.06)` 微阴影
- 深:`rgba(120,120,128,0.36)` + inset 0.5pt 0.08 白描边 + `0 0.5 1 rgba(0,0,0,0.2)`
- 内容:`value` 11.5pt 主色 + 9pt 9pt `chevron.up.down` icon 0.7 opacity 右侧
- 文本格式:`Icon + Percentage · 图标 + 百分比`(中文用 · 分隔,**这是 prototype 把双语写进 Dropdown value 的方式,需要严格照做**)

**SwiftUI 实现**:`Picker(...)` `.pickerStyle(.menu)` 系统会渲染成这样,文本里直接 `Text("Icon + Percentage · 图标 + 百分比")`。

#### CheckboxGroup / CheckItem

- 13pt × 13pt 圆角 3.5pt 复选框
- 未选:transparent + inset 1pt strong separator
- 已选:accent 填充 + 白色对勾 SVG
- 旁边 label 11.5pt
- group VStack gap 4pt

#### RadioGroup / RadioItem

- 13pt × 13pt 圆 7pt 半径
- 未选:transparent + inset 1pt strong separator
- 已选:外圈 4pt accent + 内白圆(用 inset 模拟 donut)
- 旁边 label 11.5pt
- group VStack gap 4pt

#### Slider

- 140pt × 14pt 容器
- 轨道:flex 1,高 3pt 圆角 1.5pt,bg `.fill`,进度部分 accent
- thumb:14pt × 14pt 白圆,阴影 `0 1 3 rgba(0,0,0,0.3)` + inset 0.5pt 0.1 黑

**SwiftUI 实现**:`Slider(value: ...)` 已经长这样,无需自绘。

### 4.5 各 PrefsGroup 内容

#### Accounts · 账号

desc: `Auto-detected on your Mac. Toggle which services to display. · 自动检测,自行勾选要显示的`

行结构:`AccountRow`(非 PrefsRow,padding 12pt 上下 / 14pt 左右,gap 11pt):
- 28pt squircle radius 7pt 服务色 + 14pt 白 logo
- VStack flex 1(line-height 1.2):
  - `Codex · OpenAI · GPT-5` — 13pt .semibold,`· OpenAI · GPT-5` 部分 tertiary .regular
  - `user@example.com · Plus` — 10.5pt secondary
- 右侧 HStack gap 8pt:
  - `Connected · 已连接` 状态(6pt 圆 green + 10.5pt green 文字)
  - (仅 Codex 主账号,且已检测到时)`gift` 图标按钮(12pt,展开态 accent 色 / 收起态 secondary),点击展开/收起行下方的额外重置 credit 明细,复用 `CodexResetCreditsDetailView`,取数走 `AppState.fetchCodexResetCredits()`(懒加载,不持久化)
  - Toggle(默认开启)

未检测到时:状态显示 `Not detected · 未检测到`(systemOrange 圆 + 文字),Toggle 灰禁用,不显示 `gift` 按钮。

#### Other Codex Accounts · 其他 Codex 账号

desc: `Paste auth.json to monitor additional Codex accounts (view only). · 粘贴 auth.json 添加更多 Codex 账号额度，仅查看`

列表行(非 PrefsRow,padding 10pt 上下 / 14pt 左右):
- 左:`line.3.horizontal` 拖拽手柄(11pt tertiary),整行支持 `.draggable` / `.dropDestination` 重排序;拖拽中源行透明度 0.4,目标行顶部显示 2pt accent 高亮线
- 别名(12.5pt 常规) + 邮箱 / plan(10.5pt secondary)，各占 flex
- 右: `gift` 图标按钮(12pt,展开态 accent 色 / 收起态 secondary) + Toggle(显示/隐藏开关) + `minus.circle` 删除按钮

`gift` 按钮点击展开/收起该行下方的额外重置 credit 明细(懒加载,结果按账号 id 缓存在内存,不持久化)：展开时若无缓存或此前缓存是失败态则联网请求,成功态缓存后再次展开直接复用、不重复联网;加载中显示小号 `ProgressView` + 「加载中…」；成功显示「可用额外重置 N 次」+ 逐条 `title` / 本地时区固定 `yyyy-MM-dd HH:mm` 格式的 `过期 …` 时间;失败显示红色错误文字,收起再展开可重试。明细 UI 抽为共享组件 `CodexResetCreditsDetailView`,主账号行(「账号」组 Codex 行)与导入行复用同一份。

底部固定行:「+ 添加 Codex 账号」绿色 `plus.circle.fill` + 文字，点击弹出添加 Sheet。
空状态时只显示该行 + 说明文字。

**添加 Sheet**（460 × 460pt，modal sheet）:
1. JSON 粘贴框(TextEditor 120pt 高,等宽字体,成功时边框绿色 / 失败时红色),说明文字明确支持单个 `auth.json` 或多账号 JSON 数组(如 cc-switch 导出),并说明显示名取邮箱 @ 前部分
2. 解析成功后显示"预览"行(email · plan · account_id truncated);数组形式批量粘贴时展示多行
3. 显示开关 Toggle(单账号 / 批量通用,默认开启)
4. 错误文字区(红色)
5. 底部按钮:Cancel / Save（批量时为 Import All，保存后关闭 Sheet）

列表行的显示名优先用 `alias`,为空时取 `email` @ 前缀(老数据兼容),都没有则回退到 account id。

> Popover「其他账号」分区的字重、配色与默认 Codex 块一致(不加粗、不区分识别色)，仅文案/进度条共用 §4.3 状态色逻辑，tint 走 `Color.codexAccent`。
>
> 展示层去重:Codex 主卡片会展示时(`includesCodex`),「其他账号」区隐藏与当前 CLI 主账号同身份的镜像项(`OtherCodexAccountsSection(dedupPrimary: true)`),并按去重后的计数决定分区与其前置 Divider 是否出现,避免悬空分隔线;Codex 服务被关闭、导入账号单独成区时不去重(避免唯一账号消失)。详见技术实现 §14.8。

#### Menu Bar · 菜单栏

desc: `What appears next to the icon. · 图标旁显示什么`

行:
1. `Show in menu bar · 显示在菜单栏` → Toggle
2. `Display · 显示内容` → DropdownPill,8 个选项:
   `Icon only · 仅图标` / `Icon + Percentage · 图标 + 百分比` / `Two pills · 双 pill` / `Mini ring + % · 小环+百分比` / `Dual bars · 双条` / `Text only · 纯文字` / `State color · 状态色` / `5h ↔ Weekly chip · 5h / 周轮播`
3. `Show service · 显示服务` → CheckboxGroup(Codex / Claude Code / Antigravity)
4. `Quota period · 额度周期` + desc `Which window to display in the menu bar. · 菜单栏显示哪个窗口` → RadioGroup(`Main · 主要` / `Weekly · 周额度` / `Both · 两者`)

#### Floating HUD · 桌面悬浮窗

desc: `A small always-on-top window pinned to your desktop. · 桌面常驻的小悬浮窗`

行:
1. `Show floating window · 显示悬浮窗` → Toggle
2. `Show service · 显示哪些` → CheckboxGroup(Codex / Claude Code / Antigravity)
3. `Style · 样式` → DropdownPill,8 个 variant(见 §2.3)
4. `Position · 位置` → DropdownPill,5 项:`Top right · 右上` / `Top left · 左上` / `Bottom right · 右下` / `Bottom left · 左下` / `Last dragged · 上次拖动位置`
5. `Opacity when idle · 空闲时透明度` → Slider 0.3–1.0,默认 0.7

#### Refresh · 刷新

desc: `How often the app polls usage in the background. · 后台轮询用量的频率`

行:
1. `Auto refresh · 自动刷新` → Toggle
2. `Interval · 间隔` → DropdownPill,5 项:`1m · 1 分钟` / `2m · 2 分钟` / `3m · 3 分钟` / `5m · 5 分钟` / `10m · 10 分钟`,默认 `2m`
3. `Last refresh · 上次刷新` → 11.5pt tabular secondary 文本:`14:42:08 · 32s ago`(实时从所有主 Provider 的 `lastSuccessAt` 计算)

注:cc-bar 当前有两个 interval(`quotaInterval` / `usageInterval`),prototype 只画了一个。**实现时拆成两行**:
- `Quota refresh · 额度刷新` → 5 档 interval，默认 `2m`
- `Log scan · 日志扫描` → 5 档 interval，新安装默认 `5m`；已有用户保留已保存选择

此外还有:
- `Reset time · 重置时间` → DropdownPill,`Remaining · 剩余时长` / `Exact time · 具体时间`;Popover reset 时间 hover 时显示相反格式(剩余时长 ↔ 具体时刻)
- `Service status dot · 服务状态圆点` → Toggle,desc `Show OpenAI / Anthropic status next to each service in the popover. · 在弹出窗口为每个服务显示官方状态页圆点`,绑定 `SettingsStore.shared.showServiceStatus`(默认开)。关闭后 Popover 不画圆点,但后台 5 分钟一次的 statuspage 拉取仍照常,以便随时打开就有数据。

#### General · 通用

(无 desc)

行:
1. `Privacy mode · 隐私模式` → Toggle,desc `Hide email in the popover and account names for other Codex accounts. · 弹出窗口中隐藏主账号邮箱,并隐藏 Codex 副账号名称`,绑定 `SettingsStore.shared.privacyMode`(默认开)。开启后 Popover 主账号 subtitle 不显示邮箱(只剩 plan 或 brand 兜底);Codex 副账号名称行整行隐藏,subtitle 只保留 plan,并由 HStack 自动垂直居中。设置页与导入页不受影响。
2. `Launch at login · 开机自动启动` → Toggle(连 `SMAppService.mainApp`)，开关状态以系统 `SMAppService.mainApp.status` 为准；注册失败时回滚到系统真实状态并显示可操作错误提示，`.requiresApproval` 时提示用户到系统设置批准。
3. `Show in Dock · 在 Dock 中显示` → Toggle(默认关)
4. `Language · 语言` → DropdownPill,3 项:`System · 跟随系统` / `English` / `中文 · Chinese`

### 4.6 底部 footer

- marginTop 24pt,padding `12pt 上下`,HStack gap 8pt,11pt tertiary
- `CCBar {version} · multi-service quota & local usage stats`(中英文同义,均以 `CCBar` 为 App 名,不用 `cc-bar`)

### 4.7 浅深色

- Window background:系统 Window(浅 `#ECECEE` / 深 `#1E1E1F`)。
- PrefsGroup body:按 设计风格 §12.3 切换。
- 控件:用系统语义色,自动适配。
- Toggle 打开态绿色:系统 `Color.green` 自动切换 `#30A14E` / `#30D158`。

### 4.8 数据来源对照

| 控件 | 绑定到 `SettingsStore.shared.` |
|---|---|
| Show Provider | `isProviderEnabled(_:)` |
| Menu Bar Show service | `isProviderShownInMenuBar(_:)` |
| Menu Bar Display | `menuBarVariant`(新字段) |
| Menu Bar Quota period | `menuBarWindow` |
| Floating Show | `floatingEnabled` + `isProviderShownInFloatingHUD(_:)` |
| Floating Style | `floatingVariant`(新字段) |
| Floating Position | `floatingPosition` |
| Floating Opacity | `floatingIdleOpacity` |
| Auto refresh | `autoRefreshEnabled`(新字段,默认 true) |
| Quota interval | `quotaInterval` |
| Log scan interval | `usageInterval` |
| Service status dot | `showServiceStatus` |
| Privacy mode | `privacyMode` |
| Launch at login | `launchAtLogin` |
| Show in Dock | `showInDock`(新字段) |
| Language | `language`(新字段:`.system / .english / .chinese`) |

---

## 5. Onboarding

> 首次启动时显示,4 步。prototype 只画了 Step 1(Welcome)和 Step 2(Detect accounts);Step 3/4 按 prototype 风格推演。

### 5.1 整体规格

| 项 | 值 |
|---|---|
| 窗口尺寸 | 620pt × 520pt 固定 |
| Toolbar | 极简,只显示 `N of 4 · 步骤名 · 中文`(11pt secondary) |
| 底部 4 点进度指示 | 居中,absolute bottom 16pt,4 个 6pt 圆点,active 用 accent,非 active 用 fill |

### 5.2 Step 1 · Welcome

居中布局(VStack 居中,padding 32pt):

1. **App 图标** 96pt × 96pt:
   - 优先加载真实 App icon(`NSApplication.shared.applicationIconImage`),`resizable().aspectRatio(.fit)`
   - 加载失败时回退到渐变方块:圆角 22pt(squircle),渐变 `155deg, #4A4A4F 0%, #6C6C70 50%, #D97757 100%`,内叠 `gauge.medium` 44pt 白色 SF Symbol + `0 1 0 rgba(255,255,255,0.18)` 内描边
   - 阴影 `0 10 30 rgba(0,0,0,0.3)`
   - marginBottom 24pt
2. **标题** `Welcome to CCBar` 22pt .bold kerning -0.4 line-height 1.15
3. **中文副标** `欢迎使用 CCBar` 14pt secondary marginTop 2pt
4. **描述段** marginTop 14pt,maxWidth 360pt,line-height 1.5:
   - EN 13pt secondary:`Track Codex, Claude Code, and Antigravity quota right from your menu bar.`
   - CN 12pt secondary opacity 0.85:`在菜单栏即时查看 Codex、Claude Code 与 Antigravity 的额度。`
5. **按钮区** marginTop 24pt,VStack gap 8pt 居中:
   - `Get started · 开始` PrimaryButton
   - `What's new in 1.0 · 新功能` accent text button(无边框)

### 5.3 Step 2 · Detect accounts

Padding 28pt,VStack 顶对齐:

1. **标题**:单行,按检测结果二选一,不再有独立副标行:
   - 检测到至少一个账号:`We found these accounts · 检测到以下账号` 18pt .bold kerning -0.3
   - 一个账号都没检测到:`No accounts detected yet · 未检测到账号,可稍后在设置中查看` 18pt .bold kerning -0.3
2. **账号列表** marginTop 18pt,VStack gap 10pt。Codex / Claude / Antigravity 各一行 `DetectedAccount`;Antigravity 的 source 显示 `Local Language Server`:
   - 圆角 12pt,padding 14pt 上下 / 16pt 左右,背景按 设计风格 §12.3,内描边 0.5pt
   - HStack gap 13pt:
     - 13pt 状态图标(纯展示,不可点击):检测到用 `checkmark.circle.fill` accentColor,未检测到用空心 `circle` secondary
     - 34pt × 34pt squircle 圆角 8pt 服务色 + 16pt 白 logo
     - VStack flex 1:
       - `Codex · OpenAI · GPT-5 · Plus` 13pt .semibold(`· ...` 部分 tertiary)
       - `user@example.com`(未检测到时显示 `Not detected · 未检测到`) 11.5pt secondary marginTop 1pt
       - `~/.codex/auth.json` 10pt tertiary mono marginTop 2pt
   - 未检测到 / 备选账号:opacity 0.6
3. **信息卡片** marginTop 18pt:
   - 圆角 10pt,padding `10pt 上下 / 14pt 左右`
   - 浅:`rgba(0,122,255,0.08)` + inset 0.5pt `rgba(0,122,255,0.2)`
   - 深:`rgba(10,132,255,0.12)` + inset 0.5pt `rgba(10,132,255,0.3)`
   - HStack gap 10pt:
     - 14pt info icon(`info.circle`)accent 色 flexShrink 0
     - VStack:
       - `Read-only access · 仅读取` 11.5pt primary .medium
       - `CCBar reads quota status locally. It never sends your credentials anywhere. · CCBar 仅本地读取额度,不会向任何地方发送你的凭据。` 11.5pt secondary marginTop 2pt
4. **底部按钮区**(marginTop auto,paddingTop 18pt,HStack gap 8pt):
   - 左:`Back · 上一步` SecondaryButton
   - Spacer
   - 右:`Add manually · 手动添加` SecondaryButton + `Continue · 继续` PrimaryButton

### 5.4 Step 3 · Configure menu bar + HUD(推演)

prototype 未画。建议:

- 上半部分实时 mini preview:菜单栏图标 + 桌面 HUD 缩略图,跟随下方设置变化
- 下半部分:Menu Bar Display(同 §4.5)+ Floating HUD enable + Style(同 §4.5)
- 底部:`Back / Continue`

### 5.5 Step 4 · Ready

居中(同 Welcome 布局):

- 96pt 图标(变化:勾选 `checkmark.circle.fill` accent 色 overlay)
- `You're all set · 一切就绪` 22pt .bold
- 描述:`Open Statistics now, or just keep an eye on the menu bar. · 现在打开统计,或者直接看菜单栏。`
- 按钮:`Open Statistics · 打开统计` PrimaryButton + `Close · 关闭` SecondaryButton

### 5.6 PrimaryButton / SecondaryButton 规格

**PrimaryButton**:

- 26pt 高 / 16pt 左右 padding / 圆角 7pt
- accent 填充,白字 12.5pt .medium
- 阴影 `0 0.5 0 rgba(0,0,0,0.15)` + `0 1 3 rgba(0,122,255,0.25)`

**SecondaryButton**:

- 26pt 高 / 12pt 左右 padding / 圆角 7pt
- 浅:`#fff` + inset 0.5pt 0.16 黑描边 + `0 0.5 1.5 rgba(0,0,0,0.06)` 阴影
- 深:`rgba(120,120,128,0.36)` + inset 0.5pt 0.08 白描边 + `0 0.5 1 rgba(0,0,0,0.2)` 阴影
- primary 色 12.5pt .medium

> SwiftUI 推荐:用 `.buttonStyle(.borderedProminent)` / `.borderless` 系统样式,系统在 macOS 26 上和 prototype 接近。手画就完全照上面来。

---

## 6. 已实现 / 可拓展

本文档描述的界面均已实现。下面只记录可拓展但尚未做的方向,新需求落地时再决定:

- Floating HUD 备选变体(prototype 共 8 种,当前只实现默认的 Two-row pill)。
- Stats sidebar 的 Breakdown 视图(目前灰显占位)。
- Floating HUD 单击行跳转主窗口对应服务 scope。
- HUD idle 透明度滑杆(prototype 设计有,当前仍是恒定 1.0)。

新增界面或大幅修改现有界面时,优先沿用本文档定义的尺寸 / 间距 / 配色 / 状态规则,改完后回头同步本文。
